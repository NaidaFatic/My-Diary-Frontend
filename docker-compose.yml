version: '1'
services:
  nginx-react:
    image: reactapp:latest
    container_name: onlinediary
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:80
    enviroment:
      NODE_ENV: production 